//! # Trident Generated Types
//!
//! This file is automatically generated by Trident.
//! **DO NOT EDIT THIS FILE MANUALLY**

#![allow(dead_code)]
#![allow(unused_imports)]

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use trident_fuzz::fuzzing::*;

// ============================================================================
// PROGRAM MODULES
// ============================================================================

// ----------------------------------------------------------------------------
// Program: sss_core
// ----------------------------------------------------------------------------
pub mod sss_core {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for sss_core
    pub fn program_id() -> Pubkey {
        pubkey!("2Vh56aB6CX2SsHyLVwGQpt8Z9jWqYKNzjf5BXjgM2F5y")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AcceptAuthorityTransfer
    // ....................................................................

    /// Main instruction struct for AcceptAuthorityTransfer
    pub struct AcceptAuthorityTransferInstruction {
        pub accounts: AcceptAuthorityTransferInstructionAccountMetas,
        pub data: AcceptAuthorityTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AcceptAuthorityTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct AcceptAuthorityTransferInstructionAccountMetas {
        pub pending_authority: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,
    }

    /// Account pubkeys for AcceptAuthorityTransfer instruction
    #[derive(Debug, Clone)]
    pub struct AcceptAuthorityTransferInstructionAccounts {
        pub pending_authority: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,
    }

    impl AcceptAuthorityTransferInstructionAccounts {
        pub fn new(pending_authority: Pubkey, config: Pubkey, mint: Pubkey) -> Self {
            Self {
                pending_authority,

                config,

                mint,
            }
        }
    }

    /// Instruction data for AcceptAuthorityTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AcceptAuthorityTransferInstructionData {}

    impl AcceptAuthorityTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for AcceptAuthorityTransferInstruction
    impl AcceptAuthorityTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [239u8, 248u8, 177u8, 2u8, 206u8, 97u8, 46u8, 255u8]
        }

        pub fn data(data: AcceptAuthorityTransferInstructionData) -> Self {
            Self {
                accounts: AcceptAuthorityTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AcceptAuthorityTransferInstructionAccounts) -> Self {
            self.accounts.pending_authority = AccountMeta::new(accounts.pending_authority, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pending_authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: AddMinter
    // ....................................................................

    /// Main instruction struct for AddMinter
    pub struct AddMinterInstruction {
        pub accounts: AddMinterInstructionAccountMetas,
        pub data: AddMinterInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AddMinter instruction
    #[derive(Debug, Clone, Default)]
    pub struct AddMinterInstructionAccountMetas {
        pub minter_authority: AccountMeta,

        pub config: AccountMeta,

        pub minter_config: AccountMeta,

        pub mint: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for AddMinter instruction
    #[derive(Debug, Clone)]
    pub struct AddMinterInstructionAccounts {
        pub minter_authority: Pubkey,

        pub config: Pubkey,

        pub minter_config: Pubkey,

        pub mint: Pubkey,
    }

    impl AddMinterInstructionAccounts {
        pub fn new(
            minter_authority: Pubkey,

            config: Pubkey,

            minter_config: Pubkey,

            mint: Pubkey,
        ) -> Self {
            Self {
                minter_authority,

                config,

                minter_config,

                mint,
            }
        }
    }

    /// Instruction data for AddMinter
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AddMinterInstructionData {
        pub minter: Pubkey,

        pub quota_per_period: u64,

        pub period_seconds: i64,
    }

    impl AddMinterInstructionData {
        pub fn new(minter: Pubkey, quota_per_period: u64, period_seconds: i64) -> Self {
            Self {
                minter,

                quota_per_period,

                period_seconds,
            }
        }
    }

    /// Implementation for AddMinterInstruction
    impl AddMinterInstruction {
        fn discriminator() -> [u8; 8] {
            [75u8, 86u8, 218u8, 40u8, 219u8, 6u8, 141u8, 29u8]
        }

        pub fn data(data: AddMinterInstructionData) -> Self {
            Self {
                accounts: AddMinterInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AddMinterInstructionAccounts) -> Self {
            self.accounts.minter_authority = AccountMeta::new(accounts.minter_authority, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.minter_config = AccountMeta::new(accounts.minter_config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.minter_authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.minter_config.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: AddToBlacklist
    // ....................................................................

    /// Main instruction struct for AddToBlacklist
    pub struct AddToBlacklistInstruction {
        pub accounts: AddToBlacklistInstructionAccountMetas,
        pub data: AddToBlacklistInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AddToBlacklist instruction
    #[derive(Debug, Clone, Default)]
    pub struct AddToBlacklistInstructionAccountMetas {
        pub blacklister: AccountMeta,

        pub config: AccountMeta,

        pub blacklist_entry: AccountMeta,

        pub target_account: AccountMeta,

        pub mint: AccountMeta,

        pub system_program: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for AddToBlacklist instruction
    #[derive(Debug, Clone)]
    pub struct AddToBlacklistInstructionAccounts {
        pub blacklister: Pubkey,

        pub config: Pubkey,

        pub blacklist_entry: Pubkey,

        pub target_account: Pubkey,

        pub mint: Pubkey,
    }

    impl AddToBlacklistInstructionAccounts {
        pub fn new(
            blacklister: Pubkey,

            config: Pubkey,

            blacklist_entry: Pubkey,

            target_account: Pubkey,

            mint: Pubkey,
        ) -> Self {
            Self {
                blacklister,

                config,

                blacklist_entry,

                target_account,

                mint,
            }
        }
    }

    /// Instruction data for AddToBlacklist
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AddToBlacklistInstructionData {
        pub target: Pubkey,

        pub reason: String,
    }

    impl AddToBlacklistInstructionData {
        pub fn new(target: Pubkey, reason: String) -> Self {
            Self { target, reason }
        }
    }

    /// Implementation for AddToBlacklistInstruction
    impl AddToBlacklistInstruction {
        fn discriminator() -> [u8; 8] {
            [90u8, 115u8, 98u8, 231u8, 173u8, 119u8, 117u8, 176u8]
        }

        pub fn data(data: AddToBlacklistInstructionData) -> Self {
            Self {
                accounts: AddToBlacklistInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AddToBlacklistInstructionAccounts) -> Self {
            self.accounts.blacklister = AccountMeta::new(accounts.blacklister, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.blacklist_entry = AccountMeta::new(accounts.blacklist_entry, false);

            self.accounts.target_account = AccountMeta::new(accounts.target_account, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.blacklister.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.blacklist_entry.clone());

            metas.push(self.accounts.target_account.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Burn
    // ....................................................................

    /// Main instruction struct for Burn
    pub struct BurnInstruction {
        pub accounts: BurnInstructionAccountMetas,
        pub data: BurnInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Burn instruction
    #[derive(Debug, Clone, Default)]
    pub struct BurnInstructionAccountMetas {
        pub burner: AccountMeta,

        pub config: AccountMeta,

        pub source: AccountMeta,

        pub mint: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Burn instruction
    #[derive(Debug, Clone)]
    pub struct BurnInstructionAccounts {
        pub burner: Pubkey,

        pub config: Pubkey,

        pub source: Pubkey,

        pub mint: Pubkey,
    }

    impl BurnInstructionAccounts {
        pub fn new(burner: Pubkey, config: Pubkey, source: Pubkey, mint: Pubkey) -> Self {
            Self {
                burner,

                config,

                source,

                mint,
            }
        }
    }

    /// Instruction data for Burn
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct BurnInstructionData {
        pub amount: u64,
    }

    impl BurnInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for BurnInstruction
    impl BurnInstruction {
        fn discriminator() -> [u8; 8] {
            [116u8, 110u8, 29u8, 56u8, 107u8, 219u8, 42u8, 93u8]
        }

        pub fn data(data: BurnInstructionData) -> Self {
            Self {
                accounts: BurnInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: BurnInstructionAccounts) -> Self {
            self.accounts.burner = AccountMeta::new(accounts.burner, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.source = AccountMeta::new(accounts.source, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.burner.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.source.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: FreezeAccount
    // ....................................................................

    /// Main instruction struct for FreezeAccount
    pub struct FreezeAccountInstruction {
        pub accounts: FreezeAccountInstructionAccountMetas,
        pub data: FreezeAccountInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for FreezeAccount instruction
    #[derive(Debug, Clone, Default)]
    pub struct FreezeAccountInstructionAccountMetas {
        pub authority: AccountMeta,

        pub config: AccountMeta,

        pub account: AccountMeta,

        pub mint: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for FreezeAccount instruction
    #[derive(Debug, Clone)]
    pub struct FreezeAccountInstructionAccounts {
        pub authority: Pubkey,

        pub config: Pubkey,

        pub account: Pubkey,

        pub mint: Pubkey,
    }

    impl FreezeAccountInstructionAccounts {
        pub fn new(authority: Pubkey, config: Pubkey, account: Pubkey, mint: Pubkey) -> Self {
            Self {
                authority,

                config,

                account,

                mint,
            }
        }
    }

    /// Instruction data for FreezeAccount
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct FreezeAccountInstructionData {}

    impl FreezeAccountInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for FreezeAccountInstruction
    impl FreezeAccountInstruction {
        fn discriminator() -> [u8; 8] {
            [253u8, 75u8, 82u8, 133u8, 167u8, 238u8, 43u8, 130u8]
        }

        pub fn data(data: FreezeAccountInstructionData) -> Self {
            Self {
                accounts: FreezeAccountInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: FreezeAccountInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.config = AccountMeta::new_readonly(accounts.config, false);

            self.accounts.account = AccountMeta::new(accounts.account, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.account.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Initialize
    // ....................................................................

    /// Main instruction struct for Initialize
    pub struct InitializeInstruction {
        pub accounts: InitializeInstructionAccountMetas,
        pub data: InitializeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Initialize instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeInstructionAccountMetas {
        pub payer: AccountMeta,

        pub master_authority: AccountMeta,

        pub mint: AccountMeta,

        pub config: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for Initialize instruction
    #[derive(Debug, Clone)]
    pub struct InitializeInstructionAccounts {
        pub payer: Pubkey,

        pub master_authority: Pubkey,

        pub mint: Pubkey,

        pub config: Pubkey,
    }

    impl InitializeInstructionAccounts {
        pub fn new(payer: Pubkey, master_authority: Pubkey, mint: Pubkey, config: Pubkey) -> Self {
            Self {
                payer,

                master_authority,

                mint,

                config,
            }
        }
    }

    /// Instruction data for Initialize
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeInstructionData {
        pub params: InitializeParams,
    }

    impl InitializeInstructionData {
        pub fn new(params: InitializeParams) -> Self {
            Self { params }
        }
    }

    /// Implementation for InitializeInstruction
    impl InitializeInstruction {
        fn discriminator() -> [u8; 8] {
            [175u8, 175u8, 109u8, 31u8, 13u8, 152u8, 155u8, 237u8]
        }

        pub fn data(data: InitializeInstructionData) -> Self {
            Self {
                accounts: InitializeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeInstructionAccounts) -> Self {
            self.accounts.payer = AccountMeta::new(accounts.payer, true);

            self.accounts.master_authority =
                AccountMeta::new_readonly(accounts.master_authority, true);

            self.accounts.mint = AccountMeta::new(accounts.mint, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.payer.clone());

            metas.push(self.accounts.master_authority.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Mint
    // ....................................................................

    /// Main instruction struct for Mint
    pub struct MintInstruction {
        pub accounts: MintInstructionAccountMetas,
        pub data: MintInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Mint instruction
    #[derive(Debug, Clone, Default)]
    pub struct MintInstructionAccountMetas {
        pub minter: AccountMeta,

        pub config: AccountMeta,

        pub minter_config: AccountMeta,

        pub mint: AccountMeta,

        pub destination: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Mint instruction
    #[derive(Debug, Clone)]
    pub struct MintInstructionAccounts {
        pub minter: Pubkey,

        pub config: Pubkey,

        pub minter_config: Pubkey,

        pub mint: Pubkey,

        pub destination: Pubkey,
    }

    impl MintInstructionAccounts {
        pub fn new(
            minter: Pubkey,

            config: Pubkey,

            minter_config: Pubkey,

            mint: Pubkey,

            destination: Pubkey,
        ) -> Self {
            Self {
                minter,

                config,

                minter_config,

                mint,

                destination,
            }
        }
    }

    /// Instruction data for Mint
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MintInstructionData {
        pub amount: u64,
    }

    impl MintInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for MintInstruction
    impl MintInstruction {
        fn discriminator() -> [u8; 8] {
            [51u8, 57u8, 225u8, 47u8, 182u8, 146u8, 137u8, 166u8]
        }

        pub fn data(data: MintInstructionData) -> Self {
            Self {
                accounts: MintInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MintInstructionAccounts) -> Self {
            self.accounts.minter = AccountMeta::new(accounts.minter, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.minter_config = AccountMeta::new(accounts.minter_config, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.destination = AccountMeta::new(accounts.destination, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.minter.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.minter_config.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.destination.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Pause
    // ....................................................................

    /// Main instruction struct for Pause
    pub struct PauseInstruction {
        pub accounts: PauseInstructionAccountMetas,
        pub data: PauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Pause instruction
    #[derive(Debug, Clone, Default)]
    pub struct PauseInstructionAccountMetas {
        pub pauser: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,
    }

    /// Account pubkeys for Pause instruction
    #[derive(Debug, Clone)]
    pub struct PauseInstructionAccounts {
        pub pauser: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,
    }

    impl PauseInstructionAccounts {
        pub fn new(pauser: Pubkey, config: Pubkey, mint: Pubkey) -> Self {
            Self {
                pauser,

                config,

                mint,
            }
        }
    }

    /// Instruction data for Pause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct PauseInstructionData {}

    impl PauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for PauseInstruction
    impl PauseInstruction {
        fn discriminator() -> [u8; 8] {
            [211u8, 22u8, 221u8, 251u8, 74u8, 121u8, 193u8, 47u8]
        }

        pub fn data(data: PauseInstructionData) -> Self {
            Self {
                accounts: PauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: PauseInstructionAccounts) -> Self {
            self.accounts.pauser = AccountMeta::new(accounts.pauser, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.pauser.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeAuthorityTransfer
    // ....................................................................

    /// Main instruction struct for ProposeAuthorityTransfer
    pub struct ProposeAuthorityTransferInstruction {
        pub accounts: ProposeAuthorityTransferInstructionAccountMetas,
        pub data: ProposeAuthorityTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeAuthorityTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeAuthorityTransferInstructionAccountMetas {
        pub master_authority: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,
    }

    /// Account pubkeys for ProposeAuthorityTransfer instruction
    #[derive(Debug, Clone)]
    pub struct ProposeAuthorityTransferInstructionAccounts {
        pub master_authority: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,
    }

    impl ProposeAuthorityTransferInstructionAccounts {
        pub fn new(master_authority: Pubkey, config: Pubkey, mint: Pubkey) -> Self {
            Self {
                master_authority,

                config,

                mint,
            }
        }
    }

    /// Instruction data for ProposeAuthorityTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeAuthorityTransferInstructionData {
        pub new_authority: Pubkey,
    }

    impl ProposeAuthorityTransferInstructionData {
        pub fn new(new_authority: Pubkey) -> Self {
            Self { new_authority }
        }
    }

    /// Implementation for ProposeAuthorityTransferInstruction
    impl ProposeAuthorityTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [57u8, 206u8, 225u8, 129u8, 35u8, 111u8, 174u8, 145u8]
        }

        pub fn data(data: ProposeAuthorityTransferInstructionData) -> Self {
            Self {
                accounts: ProposeAuthorityTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeAuthorityTransferInstructionAccounts) -> Self {
            self.accounts.master_authority = AccountMeta::new(accounts.master_authority, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.master_authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RemoveFromBlacklist
    // ....................................................................

    /// Main instruction struct for RemoveFromBlacklist
    pub struct RemoveFromBlacklistInstruction {
        pub accounts: RemoveFromBlacklistInstructionAccountMetas,
        pub data: RemoveFromBlacklistInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RemoveFromBlacklist instruction
    #[derive(Debug, Clone, Default)]
    pub struct RemoveFromBlacklistInstructionAccountMetas {
        pub blacklister: AccountMeta,

        pub config: AccountMeta,

        pub blacklist_entry: AccountMeta,

        pub target_account: AccountMeta,

        pub mint: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for RemoveFromBlacklist instruction
    #[derive(Debug, Clone)]
    pub struct RemoveFromBlacklistInstructionAccounts {
        pub blacklister: Pubkey,

        pub config: Pubkey,

        pub blacklist_entry: Pubkey,

        pub target_account: Pubkey,

        pub mint: Pubkey,
    }

    impl RemoveFromBlacklistInstructionAccounts {
        pub fn new(
            blacklister: Pubkey,

            config: Pubkey,

            blacklist_entry: Pubkey,

            target_account: Pubkey,

            mint: Pubkey,
        ) -> Self {
            Self {
                blacklister,

                config,

                blacklist_entry,

                target_account,

                mint,
            }
        }
    }

    /// Instruction data for RemoveFromBlacklist
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RemoveFromBlacklistInstructionData {
        pub target: Pubkey,
    }

    impl RemoveFromBlacklistInstructionData {
        pub fn new(target: Pubkey) -> Self {
            Self { target }
        }
    }

    /// Implementation for RemoveFromBlacklistInstruction
    impl RemoveFromBlacklistInstruction {
        fn discriminator() -> [u8; 8] {
            [47u8, 105u8, 20u8, 10u8, 165u8, 168u8, 203u8, 219u8]
        }

        pub fn data(data: RemoveFromBlacklistInstructionData) -> Self {
            Self {
                accounts: RemoveFromBlacklistInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RemoveFromBlacklistInstructionAccounts) -> Self {
            self.accounts.blacklister = AccountMeta::new(accounts.blacklister, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.blacklist_entry = AccountMeta::new(accounts.blacklist_entry, false);

            self.accounts.target_account = AccountMeta::new(accounts.target_account, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.blacklister.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.blacklist_entry.clone());

            metas.push(self.accounts.target_account.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RemoveMinter
    // ....................................................................

    /// Main instruction struct for RemoveMinter
    pub struct RemoveMinterInstruction {
        pub accounts: RemoveMinterInstructionAccountMetas,
        pub data: RemoveMinterInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RemoveMinter instruction
    #[derive(Debug, Clone, Default)]
    pub struct RemoveMinterInstructionAccountMetas {
        pub minter_authority: AccountMeta,

        pub config: AccountMeta,

        pub minter_config: AccountMeta,

        pub mint: AccountMeta,
    }

    /// Account pubkeys for RemoveMinter instruction
    #[derive(Debug, Clone)]
    pub struct RemoveMinterInstructionAccounts {
        pub minter_authority: Pubkey,

        pub config: Pubkey,

        pub minter_config: Pubkey,

        pub mint: Pubkey,
    }

    impl RemoveMinterInstructionAccounts {
        pub fn new(
            minter_authority: Pubkey,

            config: Pubkey,

            minter_config: Pubkey,

            mint: Pubkey,
        ) -> Self {
            Self {
                minter_authority,

                config,

                minter_config,

                mint,
            }
        }
    }

    /// Instruction data for RemoveMinter
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RemoveMinterInstructionData {
        pub minter: Pubkey,
    }

    impl RemoveMinterInstructionData {
        pub fn new(minter: Pubkey) -> Self {
            Self { minter }
        }
    }

    /// Implementation for RemoveMinterInstruction
    impl RemoveMinterInstruction {
        fn discriminator() -> [u8; 8] {
            [241u8, 69u8, 84u8, 16u8, 164u8, 232u8, 131u8, 79u8]
        }

        pub fn data(data: RemoveMinterInstructionData) -> Self {
            Self {
                accounts: RemoveMinterInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RemoveMinterInstructionAccounts) -> Self {
            self.accounts.minter_authority = AccountMeta::new(accounts.minter_authority, true);

            self.accounts.config = AccountMeta::new_readonly(accounts.config, false);

            self.accounts.minter_config = AccountMeta::new(accounts.minter_config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.minter_authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.minter_config.clone());

            metas.push(self.accounts.mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Seize
    // ....................................................................

    /// Main instruction struct for Seize
    pub struct SeizeInstruction {
        pub accounts: SeizeInstructionAccountMetas,
        pub data: SeizeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Seize instruction
    #[derive(Debug, Clone, Default)]
    pub struct SeizeInstructionAccountMetas {
        pub seizer: AccountMeta,

        pub config: AccountMeta,

        pub seizure_record: AccountMeta,

        pub source_account: AccountMeta,

        pub destination_account: AccountMeta,

        pub mint: AccountMeta,

        pub system_program: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for Seize instruction
    #[derive(Debug, Clone)]
    pub struct SeizeInstructionAccounts {
        pub seizer: Pubkey,

        pub config: Pubkey,

        pub seizure_record: Pubkey,

        pub source_account: Pubkey,

        pub destination_account: Pubkey,

        pub mint: Pubkey,
    }

    impl SeizeInstructionAccounts {
        pub fn new(
            seizer: Pubkey,

            config: Pubkey,

            seizure_record: Pubkey,

            source_account: Pubkey,

            destination_account: Pubkey,

            mint: Pubkey,
        ) -> Self {
            Self {
                seizer,

                config,

                seizure_record,

                source_account,

                destination_account,

                mint,
            }
        }
    }

    /// Instruction data for Seize
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SeizeInstructionData {
        pub amount: u64,

        pub reason: String,
    }

    impl SeizeInstructionData {
        pub fn new(amount: u64, reason: String) -> Self {
            Self { amount, reason }
        }
    }

    /// Implementation for SeizeInstruction
    impl SeizeInstruction {
        fn discriminator() -> [u8; 8] {
            [129u8, 159u8, 143u8, 31u8, 161u8, 224u8, 241u8, 84u8]
        }

        pub fn data(data: SeizeInstructionData) -> Self {
            Self {
                accounts: SeizeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SeizeInstructionAccounts) -> Self {
            self.accounts.seizer = AccountMeta::new(accounts.seizer, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.seizure_record = AccountMeta::new(accounts.seizure_record, false);

            self.accounts.source_account = AccountMeta::new(accounts.source_account, false);

            self.accounts.destination_account =
                AccountMeta::new(accounts.destination_account, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.seizer.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.seizure_record.clone());

            metas.push(self.accounts.source_account.clone());

            metas.push(self.accounts.destination_account.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ThawAccount
    // ....................................................................

    /// Main instruction struct for ThawAccount
    pub struct ThawAccountInstruction {
        pub accounts: ThawAccountInstructionAccountMetas,
        pub data: ThawAccountInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ThawAccount instruction
    #[derive(Debug, Clone, Default)]
    pub struct ThawAccountInstructionAccountMetas {
        pub authority: AccountMeta,

        pub config: AccountMeta,

        pub account: AccountMeta,

        pub mint: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for ThawAccount instruction
    #[derive(Debug, Clone)]
    pub struct ThawAccountInstructionAccounts {
        pub authority: Pubkey,

        pub config: Pubkey,

        pub account: Pubkey,

        pub mint: Pubkey,
    }

    impl ThawAccountInstructionAccounts {
        pub fn new(authority: Pubkey, config: Pubkey, account: Pubkey, mint: Pubkey) -> Self {
            Self {
                authority,

                config,

                account,

                mint,
            }
        }
    }

    /// Instruction data for ThawAccount
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ThawAccountInstructionData {}

    impl ThawAccountInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ThawAccountInstruction
    impl ThawAccountInstruction {
        fn discriminator() -> [u8; 8] {
            [115u8, 152u8, 79u8, 213u8, 213u8, 169u8, 184u8, 35u8]
        }

        pub fn data(data: ThawAccountInstructionData) -> Self {
            Self {
                accounts: ThawAccountInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ThawAccountInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.config = AccountMeta::new_readonly(accounts.config, false);

            self.accounts.account = AccountMeta::new(accounts.account, false);

            self.accounts.mint = AccountMeta::new(accounts.mint, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.account.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Unpause
    // ....................................................................

    /// Main instruction struct for Unpause
    pub struct UnpauseInstruction {
        pub accounts: UnpauseInstructionAccountMetas,
        pub data: UnpauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Unpause instruction
    #[derive(Debug, Clone, Default)]
    pub struct UnpauseInstructionAccountMetas {
        pub master_authority: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,
    }

    /// Account pubkeys for Unpause instruction
    #[derive(Debug, Clone)]
    pub struct UnpauseInstructionAccounts {
        pub master_authority: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,
    }

    impl UnpauseInstructionAccounts {
        pub fn new(master_authority: Pubkey, config: Pubkey, mint: Pubkey) -> Self {
            Self {
                master_authority,

                config,

                mint,
            }
        }
    }

    /// Instruction data for Unpause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UnpauseInstructionData {}

    impl UnpauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for UnpauseInstruction
    impl UnpauseInstruction {
        fn discriminator() -> [u8; 8] {
            [169u8, 144u8, 4u8, 38u8, 10u8, 141u8, 188u8, 255u8]
        }

        pub fn data(data: UnpauseInstructionData) -> Self {
            Self {
                accounts: UnpauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UnpauseInstructionAccounts) -> Self {
            self.accounts.master_authority = AccountMeta::new(accounts.master_authority, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.master_authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateRoles
    // ....................................................................

    /// Main instruction struct for UpdateRoles
    pub struct UpdateRolesInstruction {
        pub accounts: UpdateRolesInstructionAccountMetas,
        pub data: UpdateRolesInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateRoles instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateRolesInstructionAccountMetas {
        pub master_authority: AccountMeta,

        pub config: AccountMeta,

        pub mint: AccountMeta,
    }

    /// Account pubkeys for UpdateRoles instruction
    #[derive(Debug, Clone)]
    pub struct UpdateRolesInstructionAccounts {
        pub master_authority: Pubkey,

        pub config: Pubkey,

        pub mint: Pubkey,
    }

    impl UpdateRolesInstructionAccounts {
        pub fn new(master_authority: Pubkey, config: Pubkey, mint: Pubkey) -> Self {
            Self {
                master_authority,

                config,

                mint,
            }
        }
    }

    /// Instruction data for UpdateRoles
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateRolesInstructionData {
        pub update: RoleUpdate,
    }

    impl UpdateRolesInstructionData {
        pub fn new(update: RoleUpdate) -> Self {
            Self { update }
        }
    }

    /// Implementation for UpdateRolesInstruction
    impl UpdateRolesInstruction {
        fn discriminator() -> [u8; 8] {
            [220u8, 152u8, 205u8, 233u8, 177u8, 123u8, 219u8, 125u8]
        }

        pub fn data(data: UpdateRolesInstructionData) -> Self {
            Self {
                accounts: UpdateRolesInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateRolesInstructionAccounts) -> Self {
            self.accounts.master_authority = AccountMeta::new(accounts.master_authority, true);

            self.accounts.config = AccountMeta::new(accounts.config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.master_authority.clone());

            metas.push(self.accounts.config.clone());

            metas.push(self.accounts.mint.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ----------------------------------------------------------------------------
// Program: sss_oracle
// ----------------------------------------------------------------------------
pub mod sss_oracle {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for sss_oracle
    pub fn program_id() -> Pubkey {
        pubkey!("Brj7RU6jcmWXqCSfBa6o3v5bHS48Z6uDyKZUfG8ZbQoD")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AcceptAuthorityTransfer
    // ....................................................................

    /// Main instruction struct for AcceptAuthorityTransfer
    pub struct AcceptAuthorityTransferInstruction {
        pub accounts: AcceptAuthorityTransferInstructionAccountMetas,
        pub data: AcceptAuthorityTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AcceptAuthorityTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct AcceptAuthorityTransferInstructionAccountMetas {
        pub new_authority: AccountMeta,

        pub oracle_config: AccountMeta,
    }

    /// Account pubkeys for AcceptAuthorityTransfer instruction
    #[derive(Debug, Clone)]
    pub struct AcceptAuthorityTransferInstructionAccounts {
        pub new_authority: Pubkey,

        pub oracle_config: Pubkey,
    }

    impl AcceptAuthorityTransferInstructionAccounts {
        pub fn new(new_authority: Pubkey, oracle_config: Pubkey) -> Self {
            Self {
                new_authority,

                oracle_config,
            }
        }
    }

    /// Instruction data for AcceptAuthorityTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AcceptAuthorityTransferInstructionData {}

    impl AcceptAuthorityTransferInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for AcceptAuthorityTransferInstruction
    impl AcceptAuthorityTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [239u8, 248u8, 177u8, 2u8, 206u8, 97u8, 46u8, 255u8]
        }

        pub fn data(data: AcceptAuthorityTransferInstructionData) -> Self {
            Self {
                accounts: AcceptAuthorityTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AcceptAuthorityTransferInstructionAccounts) -> Self {
            self.accounts.new_authority = AccountMeta::new_readonly(accounts.new_authority, true);

            self.accounts.oracle_config = AccountMeta::new(accounts.oracle_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.new_authority.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetMintQuote
    // ....................................................................

    /// Main instruction struct for GetMintQuote
    pub struct GetMintQuoteInstruction {
        pub accounts: GetMintQuoteInstructionAccountMetas,
        pub data: GetMintQuoteInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetMintQuote instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetMintQuoteInstructionAccountMetas {
        pub requester: AccountMeta,

        pub oracle_config: AccountMeta,

        pub registry: AccountMeta,

        pub switchboard_feed: AccountMeta,

        pub quote: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for GetMintQuote instruction
    #[derive(Debug, Clone)]
    pub struct GetMintQuoteInstructionAccounts {
        pub requester: Pubkey,

        pub oracle_config: Pubkey,

        pub registry: Pubkey,

        pub switchboard_feed: Pubkey,

        pub quote: Pubkey,
    }

    impl GetMintQuoteInstructionAccounts {
        pub fn new(
            requester: Pubkey,

            oracle_config: Pubkey,

            registry: Pubkey,

            switchboard_feed: Pubkey,

            quote: Pubkey,
        ) -> Self {
            Self {
                requester,

                oracle_config,

                registry,

                switchboard_feed,

                quote,
            }
        }
    }

    /// Instruction data for GetMintQuote
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetMintQuoteInstructionData {
        pub params: GetQuoteParams,
    }

    impl GetMintQuoteInstructionData {
        pub fn new(params: GetQuoteParams) -> Self {
            Self { params }
        }
    }

    /// Implementation for GetMintQuoteInstruction
    impl GetMintQuoteInstruction {
        fn discriminator() -> [u8; 8] {
            [161u8, 124u8, 87u8, 175u8, 98u8, 25u8, 175u8, 169u8]
        }

        pub fn data(data: GetMintQuoteInstructionData) -> Self {
            Self {
                accounts: GetMintQuoteInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetMintQuoteInstructionAccounts) -> Self {
            self.accounts.requester = AccountMeta::new(accounts.requester, true);

            self.accounts.oracle_config = AccountMeta::new_readonly(accounts.oracle_config, false);

            self.accounts.registry = AccountMeta::new_readonly(accounts.registry, false);

            self.accounts.switchboard_feed =
                AccountMeta::new_readonly(accounts.switchboard_feed, false);

            self.accounts.quote = AccountMeta::new(accounts.quote, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.requester.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.push(self.accounts.registry.clone());

            metas.push(self.accounts.switchboard_feed.clone());

            metas.push(self.accounts.quote.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: GetRedeemQuote
    // ....................................................................

    /// Main instruction struct for GetRedeemQuote
    pub struct GetRedeemQuoteInstruction {
        pub accounts: GetRedeemQuoteInstructionAccountMetas,
        pub data: GetRedeemQuoteInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for GetRedeemQuote instruction
    #[derive(Debug, Clone, Default)]
    pub struct GetRedeemQuoteInstructionAccountMetas {
        pub requester: AccountMeta,

        pub oracle_config: AccountMeta,

        pub registry: AccountMeta,

        pub switchboard_feed: AccountMeta,

        pub quote: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for GetRedeemQuote instruction
    #[derive(Debug, Clone)]
    pub struct GetRedeemQuoteInstructionAccounts {
        pub requester: Pubkey,

        pub oracle_config: Pubkey,

        pub registry: Pubkey,

        pub switchboard_feed: Pubkey,

        pub quote: Pubkey,
    }

    impl GetRedeemQuoteInstructionAccounts {
        pub fn new(
            requester: Pubkey,

            oracle_config: Pubkey,

            registry: Pubkey,

            switchboard_feed: Pubkey,

            quote: Pubkey,
        ) -> Self {
            Self {
                requester,

                oracle_config,

                registry,

                switchboard_feed,

                quote,
            }
        }
    }

    /// Instruction data for GetRedeemQuote
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct GetRedeemQuoteInstructionData {
        pub params: GetQuoteParams,
    }

    impl GetRedeemQuoteInstructionData {
        pub fn new(params: GetQuoteParams) -> Self {
            Self { params }
        }
    }

    /// Implementation for GetRedeemQuoteInstruction
    impl GetRedeemQuoteInstruction {
        fn discriminator() -> [u8; 8] {
            [104u8, 228u8, 76u8, 87u8, 172u8, 49u8, 183u8, 137u8]
        }

        pub fn data(data: GetRedeemQuoteInstructionData) -> Self {
            Self {
                accounts: GetRedeemQuoteInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: GetRedeemQuoteInstructionAccounts) -> Self {
            self.accounts.requester = AccountMeta::new(accounts.requester, true);

            self.accounts.oracle_config = AccountMeta::new_readonly(accounts.oracle_config, false);

            self.accounts.registry = AccountMeta::new_readonly(accounts.registry, false);

            self.accounts.switchboard_feed =
                AccountMeta::new_readonly(accounts.switchboard_feed, false);

            self.accounts.quote = AccountMeta::new(accounts.quote, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.requester.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.push(self.accounts.registry.clone());

            metas.push(self.accounts.switchboard_feed.clone());

            metas.push(self.accounts.quote.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeOracle
    // ....................................................................

    /// Main instruction struct for InitializeOracle
    pub struct InitializeOracleInstruction {
        pub accounts: InitializeOracleInstructionAccountMetas,
        pub data: InitializeOracleInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeOracle instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeOracleInstructionAccountMetas {
        pub payer: AccountMeta,

        pub authority: AccountMeta,

        pub mint: AccountMeta,

        pub oracle_config: AccountMeta,

        pub registry: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeOracle instruction
    #[derive(Debug, Clone)]
    pub struct InitializeOracleInstructionAccounts {
        pub payer: Pubkey,

        pub authority: Pubkey,

        pub mint: Pubkey,

        pub oracle_config: Pubkey,

        pub registry: Pubkey,
    }

    impl InitializeOracleInstructionAccounts {
        pub fn new(
            payer: Pubkey,

            authority: Pubkey,

            mint: Pubkey,

            oracle_config: Pubkey,

            registry: Pubkey,
        ) -> Self {
            Self {
                payer,

                authority,

                mint,

                oracle_config,

                registry,
            }
        }
    }

    /// Instruction data for InitializeOracle
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeOracleInstructionData {
        pub params: InitializeOracleParams,
    }

    impl InitializeOracleInstructionData {
        pub fn new(params: InitializeOracleParams) -> Self {
            Self { params }
        }
    }

    /// Implementation for InitializeOracleInstruction
    impl InitializeOracleInstruction {
        fn discriminator() -> [u8; 8] {
            [144u8, 223u8, 131u8, 120u8, 196u8, 253u8, 181u8, 99u8]
        }

        pub fn data(data: InitializeOracleInstructionData) -> Self {
            Self {
                accounts: InitializeOracleInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeOracleInstructionAccounts) -> Self {
            self.accounts.payer = AccountMeta::new(accounts.payer, true);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.oracle_config = AccountMeta::new(accounts.oracle_config, false);

            self.accounts.registry = AccountMeta::new_readonly(accounts.registry, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.payer.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.push(self.accounts.registry.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeRegistry
    // ....................................................................

    /// Main instruction struct for InitializeRegistry
    pub struct InitializeRegistryInstruction {
        pub accounts: InitializeRegistryInstructionAccountMetas,
        pub data: InitializeRegistryInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeRegistry instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeRegistryInstructionAccountMetas {
        pub payer: AccountMeta,

        pub authority: AccountMeta,

        pub registry: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeRegistry instruction
    #[derive(Debug, Clone)]
    pub struct InitializeRegistryInstructionAccounts {
        pub payer: Pubkey,

        pub authority: Pubkey,

        pub registry: Pubkey,
    }

    impl InitializeRegistryInstructionAccounts {
        pub fn new(payer: Pubkey, authority: Pubkey, registry: Pubkey) -> Self {
            Self {
                payer,

                authority,

                registry,
            }
        }
    }

    /// Instruction data for InitializeRegistry
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeRegistryInstructionData {}

    impl InitializeRegistryInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeRegistryInstruction
    impl InitializeRegistryInstruction {
        fn discriminator() -> [u8; 8] {
            [189u8, 181u8, 20u8, 17u8, 174u8, 57u8, 249u8, 59u8]
        }

        pub fn data(data: InitializeRegistryInstructionData) -> Self {
            Self {
                accounts: InitializeRegistryInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeRegistryInstructionAccounts) -> Self {
            self.accounts.payer = AccountMeta::new(accounts.payer, true);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.registry = AccountMeta::new(accounts.registry, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.payer.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.registry.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MintWithOracle
    // ....................................................................

    /// Main instruction struct for MintWithOracle
    pub struct MintWithOracleInstruction {
        pub accounts: MintWithOracleInstructionAccountMetas,
        pub data: MintWithOracleInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MintWithOracle instruction
    #[derive(Debug, Clone, Default)]
    pub struct MintWithOracleInstructionAccountMetas {
        pub requester: AccountMeta,

        pub oracle_config: AccountMeta,

        pub quote: AccountMeta,
    }

    /// Account pubkeys for MintWithOracle instruction
    #[derive(Debug, Clone)]
    pub struct MintWithOracleInstructionAccounts {
        pub requester: Pubkey,

        pub oracle_config: Pubkey,

        pub quote: Pubkey,
    }

    impl MintWithOracleInstructionAccounts {
        pub fn new(requester: Pubkey, oracle_config: Pubkey, quote: Pubkey) -> Self {
            Self {
                requester,

                oracle_config,

                quote,
            }
        }
    }

    /// Instruction data for MintWithOracle
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MintWithOracleInstructionData {}

    impl MintWithOracleInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for MintWithOracleInstruction
    impl MintWithOracleInstruction {
        fn discriminator() -> [u8; 8] {
            [59u8, 36u8, 162u8, 197u8, 12u8, 79u8, 194u8, 72u8]
        }

        pub fn data(data: MintWithOracleInstructionData) -> Self {
            Self {
                accounts: MintWithOracleInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MintWithOracleInstructionAccounts) -> Self {
            self.accounts.requester = AccountMeta::new(accounts.requester, true);

            self.accounts.oracle_config = AccountMeta::new(accounts.oracle_config, false);

            self.accounts.quote = AccountMeta::new(accounts.quote, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.requester.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.push(self.accounts.quote.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: PauseOracle
    // ....................................................................

    /// Main instruction struct for PauseOracle
    pub struct PauseOracleInstruction {
        pub accounts: PauseOracleInstructionAccountMetas,
        pub data: PauseOracleInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for PauseOracle instruction
    #[derive(Debug, Clone, Default)]
    pub struct PauseOracleInstructionAccountMetas {
        pub authority: AccountMeta,

        pub oracle_config: AccountMeta,
    }

    /// Account pubkeys for PauseOracle instruction
    #[derive(Debug, Clone)]
    pub struct PauseOracleInstructionAccounts {
        pub authority: Pubkey,

        pub oracle_config: Pubkey,
    }

    impl PauseOracleInstructionAccounts {
        pub fn new(authority: Pubkey, oracle_config: Pubkey) -> Self {
            Self {
                authority,

                oracle_config,
            }
        }
    }

    /// Instruction data for PauseOracle
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct PauseOracleInstructionData {
        pub reason: String,
    }

    impl PauseOracleInstructionData {
        pub fn new(reason: String) -> Self {
            Self { reason }
        }
    }

    /// Implementation for PauseOracleInstruction
    impl PauseOracleInstruction {
        fn discriminator() -> [u8; 8] {
            [170u8, 116u8, 3u8, 121u8, 247u8, 109u8, 147u8, 191u8]
        }

        pub fn data(data: PauseOracleInstructionData) -> Self {
            Self {
                accounts: PauseOracleInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: PauseOracleInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.oracle_config = AccountMeta::new(accounts.oracle_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ProposeAuthorityTransfer
    // ....................................................................

    /// Main instruction struct for ProposeAuthorityTransfer
    pub struct ProposeAuthorityTransferInstruction {
        pub accounts: ProposeAuthorityTransferInstructionAccountMetas,
        pub data: ProposeAuthorityTransferInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ProposeAuthorityTransfer instruction
    #[derive(Debug, Clone, Default)]
    pub struct ProposeAuthorityTransferInstructionAccountMetas {
        pub authority: AccountMeta,

        pub oracle_config: AccountMeta,
    }

    /// Account pubkeys for ProposeAuthorityTransfer instruction
    #[derive(Debug, Clone)]
    pub struct ProposeAuthorityTransferInstructionAccounts {
        pub authority: Pubkey,

        pub oracle_config: Pubkey,
    }

    impl ProposeAuthorityTransferInstructionAccounts {
        pub fn new(authority: Pubkey, oracle_config: Pubkey) -> Self {
            Self {
                authority,

                oracle_config,
            }
        }
    }

    /// Instruction data for ProposeAuthorityTransfer
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ProposeAuthorityTransferInstructionData {
        pub new_authority: Pubkey,
    }

    impl ProposeAuthorityTransferInstructionData {
        pub fn new(new_authority: Pubkey) -> Self {
            Self { new_authority }
        }
    }

    /// Implementation for ProposeAuthorityTransferInstruction
    impl ProposeAuthorityTransferInstruction {
        fn discriminator() -> [u8; 8] {
            [57u8, 206u8, 225u8, 129u8, 35u8, 111u8, 174u8, 145u8]
        }

        pub fn data(data: ProposeAuthorityTransferInstructionData) -> Self {
            Self {
                accounts: ProposeAuthorityTransferInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ProposeAuthorityTransferInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.oracle_config = AccountMeta::new(accounts.oracle_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RegisterFeed
    // ....................................................................

    /// Main instruction struct for RegisterFeed
    pub struct RegisterFeedInstruction {
        pub accounts: RegisterFeedInstructionAccountMetas,
        pub data: RegisterFeedInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RegisterFeed instruction
    #[derive(Debug, Clone, Default)]
    pub struct RegisterFeedInstructionAccountMetas {
        pub payer: AccountMeta,

        pub authority: AccountMeta,

        pub registry: AccountMeta,

        pub switchboard_feed: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for RegisterFeed instruction
    #[derive(Debug, Clone)]
    pub struct RegisterFeedInstructionAccounts {
        pub payer: Pubkey,

        pub authority: Pubkey,

        pub registry: Pubkey,

        pub switchboard_feed: Pubkey,
    }

    impl RegisterFeedInstructionAccounts {
        pub fn new(
            payer: Pubkey,

            authority: Pubkey,

            registry: Pubkey,

            switchboard_feed: Pubkey,
        ) -> Self {
            Self {
                payer,

                authority,

                registry,

                switchboard_feed,
            }
        }
    }

    /// Instruction data for RegisterFeed
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RegisterFeedInstructionData {
        pub params: RegisterFeedParams,
    }

    impl RegisterFeedInstructionData {
        pub fn new(params: RegisterFeedParams) -> Self {
            Self { params }
        }
    }

    /// Implementation for RegisterFeedInstruction
    impl RegisterFeedInstruction {
        fn discriminator() -> [u8; 8] {
            [138u8, 181u8, 114u8, 65u8, 71u8, 145u8, 61u8, 111u8]
        }

        pub fn data(data: RegisterFeedInstructionData) -> Self {
            Self {
                accounts: RegisterFeedInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RegisterFeedInstructionAccounts) -> Self {
            self.accounts.payer = AccountMeta::new(accounts.payer, true);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.registry = AccountMeta::new(accounts.registry, false);

            self.accounts.switchboard_feed =
                AccountMeta::new_readonly(accounts.switchboard_feed, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.payer.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.registry.clone());

            metas.push(self.accounts.switchboard_feed.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UnpauseOracle
    // ....................................................................

    /// Main instruction struct for UnpauseOracle
    pub struct UnpauseOracleInstruction {
        pub accounts: UnpauseOracleInstructionAccountMetas,
        pub data: UnpauseOracleInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UnpauseOracle instruction
    #[derive(Debug, Clone, Default)]
    pub struct UnpauseOracleInstructionAccountMetas {
        pub authority: AccountMeta,

        pub oracle_config: AccountMeta,
    }

    /// Account pubkeys for UnpauseOracle instruction
    #[derive(Debug, Clone)]
    pub struct UnpauseOracleInstructionAccounts {
        pub authority: Pubkey,

        pub oracle_config: Pubkey,
    }

    impl UnpauseOracleInstructionAccounts {
        pub fn new(authority: Pubkey, oracle_config: Pubkey) -> Self {
            Self {
                authority,

                oracle_config,
            }
        }
    }

    /// Instruction data for UnpauseOracle
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UnpauseOracleInstructionData {}

    impl UnpauseOracleInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for UnpauseOracleInstruction
    impl UnpauseOracleInstruction {
        fn discriminator() -> [u8; 8] {
            [187u8, 113u8, 244u8, 174u8, 46u8, 2u8, 129u8, 215u8]
        }

        pub fn data(data: UnpauseOracleInstructionData) -> Self {
            Self {
                accounts: UnpauseOracleInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UnpauseOracleInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.oracle_config = AccountMeta::new(accounts.oracle_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UpdateCpiMultiplier
    // ....................................................................

    /// Main instruction struct for UpdateCpiMultiplier
    pub struct UpdateCpiMultiplierInstruction {
        pub accounts: UpdateCpiMultiplierInstructionAccountMetas,
        pub data: UpdateCpiMultiplierInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UpdateCpiMultiplier instruction
    #[derive(Debug, Clone, Default)]
    pub struct UpdateCpiMultiplierInstructionAccountMetas {
        pub authority: AccountMeta,

        pub oracle_config: AccountMeta,
    }

    /// Account pubkeys for UpdateCpiMultiplier instruction
    #[derive(Debug, Clone)]
    pub struct UpdateCpiMultiplierInstructionAccounts {
        pub authority: Pubkey,

        pub oracle_config: Pubkey,
    }

    impl UpdateCpiMultiplierInstructionAccounts {
        pub fn new(authority: Pubkey, oracle_config: Pubkey) -> Self {
            Self {
                authority,

                oracle_config,
            }
        }
    }

    /// Instruction data for UpdateCpiMultiplier
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UpdateCpiMultiplierInstructionData {
        pub params: UpdateCpiParams,
    }

    impl UpdateCpiMultiplierInstructionData {
        pub fn new(params: UpdateCpiParams) -> Self {
            Self { params }
        }
    }

    /// Implementation for UpdateCpiMultiplierInstruction
    impl UpdateCpiMultiplierInstruction {
        fn discriminator() -> [u8; 8] {
            [173u8, 77u8, 84u8, 2u8, 36u8, 149u8, 224u8, 110u8]
        }

        pub fn data(data: UpdateCpiMultiplierInstructionData) -> Self {
            Self {
                accounts: UpdateCpiMultiplierInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UpdateCpiMultiplierInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.oracle_config = AccountMeta::new(accounts.oracle_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.oracle_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ----------------------------------------------------------------------------
// Program: sss_transfer_hook
// ----------------------------------------------------------------------------
pub mod sss_transfer_hook {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for sss_transfer_hook
    pub fn program_id() -> Pubkey {
        pubkey!("9Vu21Cy4yShn7SukayuArDRy36fVzXMjsnJiTG2ZZDxF")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: DisableHook
    // ....................................................................

    /// Main instruction struct for DisableHook
    pub struct DisableHookInstruction {
        pub accounts: DisableHookInstructionAccountMetas,
        pub data: DisableHookInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for DisableHook instruction
    #[derive(Debug, Clone, Default)]
    pub struct DisableHookInstructionAccountMetas {
        pub authority: AccountMeta,

        pub hook_config: AccountMeta,
    }

    /// Account pubkeys for DisableHook instruction
    #[derive(Debug, Clone)]
    pub struct DisableHookInstructionAccounts {
        pub authority: Pubkey,

        pub hook_config: Pubkey,
    }

    impl DisableHookInstructionAccounts {
        pub fn new(authority: Pubkey, hook_config: Pubkey) -> Self {
            Self {
                authority,

                hook_config,
            }
        }
    }

    /// Instruction data for DisableHook
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct DisableHookInstructionData {}

    impl DisableHookInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for DisableHookInstruction
    impl DisableHookInstruction {
        fn discriminator() -> [u8; 8] {
            [135u8, 250u8, 78u8, 138u8, 213u8, 16u8, 28u8, 219u8]
        }

        pub fn data(data: DisableHookInstructionData) -> Self {
            Self {
                accounts: DisableHookInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: DisableHookInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.hook_config = AccountMeta::new(accounts.hook_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.hook_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: EnableHook
    // ....................................................................

    /// Main instruction struct for EnableHook
    pub struct EnableHookInstruction {
        pub accounts: EnableHookInstructionAccountMetas,
        pub data: EnableHookInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for EnableHook instruction
    #[derive(Debug, Clone, Default)]
    pub struct EnableHookInstructionAccountMetas {
        pub authority: AccountMeta,

        pub hook_config: AccountMeta,
    }

    /// Account pubkeys for EnableHook instruction
    #[derive(Debug, Clone)]
    pub struct EnableHookInstructionAccounts {
        pub authority: Pubkey,

        pub hook_config: Pubkey,
    }

    impl EnableHookInstructionAccounts {
        pub fn new(authority: Pubkey, hook_config: Pubkey) -> Self {
            Self {
                authority,

                hook_config,
            }
        }
    }

    /// Instruction data for EnableHook
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct EnableHookInstructionData {}

    impl EnableHookInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for EnableHookInstruction
    impl EnableHookInstruction {
        fn discriminator() -> [u8; 8] {
            [242u8, 81u8, 117u8, 14u8, 86u8, 35u8, 185u8, 143u8]
        }

        pub fn data(data: EnableHookInstructionData) -> Self {
            Self {
                accounts: EnableHookInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: EnableHookInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.hook_config = AccountMeta::new(accounts.hook_config, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.hook_config.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Execute
    // ....................................................................

    /// Main instruction struct for Execute
    pub struct ExecuteInstruction {
        pub accounts: ExecuteInstructionAccountMetas,
        pub data: ExecuteInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Execute instruction
    #[derive(Debug, Clone, Default)]
    pub struct ExecuteInstructionAccountMetas {
        pub source: AccountMeta,

        pub mint: AccountMeta,

        pub destination: AccountMeta,

        pub source_authority: AccountMeta,

        pub hook_config: AccountMeta,

        pub sss_core_program: AccountMeta,

        pub sender_blacklist_entry: AccountMeta,

        pub recipient_blacklist_entry: AccountMeta,
    }

    /// Account pubkeys for Execute instruction
    #[derive(Debug, Clone)]
    pub struct ExecuteInstructionAccounts {
        pub source: Pubkey,

        pub mint: Pubkey,

        pub destination: Pubkey,

        pub source_authority: Pubkey,

        pub hook_config: Pubkey,

        pub sss_core_program: Pubkey,

        pub sender_blacklist_entry: Pubkey,

        pub recipient_blacklist_entry: Pubkey,
    }

    impl ExecuteInstructionAccounts {
        pub fn new(
            source: Pubkey,

            mint: Pubkey,

            destination: Pubkey,

            source_authority: Pubkey,

            hook_config: Pubkey,

            sss_core_program: Pubkey,

            sender_blacklist_entry: Pubkey,

            recipient_blacklist_entry: Pubkey,
        ) -> Self {
            Self {
                source,

                mint,

                destination,

                source_authority,

                hook_config,

                sss_core_program,

                sender_blacklist_entry,

                recipient_blacklist_entry,
            }
        }
    }

    /// Instruction data for Execute
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ExecuteInstructionData {
        pub amount: u64,
    }

    impl ExecuteInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for ExecuteInstruction
    impl ExecuteInstruction {
        fn discriminator() -> [u8; 8] {
            [130u8, 221u8, 242u8, 154u8, 13u8, 193u8, 189u8, 29u8]
        }

        pub fn data(data: ExecuteInstructionData) -> Self {
            Self {
                accounts: ExecuteInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ExecuteInstructionAccounts) -> Self {
            self.accounts.source = AccountMeta::new_readonly(accounts.source, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.destination = AccountMeta::new_readonly(accounts.destination, false);

            self.accounts.source_authority =
                AccountMeta::new_readonly(accounts.source_authority, false);

            self.accounts.hook_config = AccountMeta::new(accounts.hook_config, false);

            self.accounts.sss_core_program =
                AccountMeta::new_readonly(accounts.sss_core_program, false);

            self.accounts.sender_blacklist_entry =
                AccountMeta::new_readonly(accounts.sender_blacklist_entry, false);

            self.accounts.recipient_blacklist_entry =
                AccountMeta::new_readonly(accounts.recipient_blacklist_entry, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.source.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.destination.clone());

            metas.push(self.accounts.source_authority.clone());

            metas.push(self.accounts.hook_config.clone());

            metas.push(self.accounts.sss_core_program.clone());

            metas.push(self.accounts.sender_blacklist_entry.clone());

            metas.push(self.accounts.recipient_blacklist_entry.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeExtraAccountMetaList
    // ....................................................................

    /// Main instruction struct for InitializeExtraAccountMetaList
    pub struct InitializeExtraAccountMetaListInstruction {
        pub accounts: InitializeExtraAccountMetaListInstructionAccountMetas,
        pub data: InitializeExtraAccountMetaListInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeExtraAccountMetaList instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeExtraAccountMetaListInstructionAccountMetas {
        pub payer: AccountMeta,

        pub extra_account_meta_list: AccountMeta,

        pub mint: AccountMeta,

        pub hook_config: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeExtraAccountMetaList instruction
    #[derive(Debug, Clone)]
    pub struct InitializeExtraAccountMetaListInstructionAccounts {
        pub payer: Pubkey,

        pub extra_account_meta_list: Pubkey,

        pub mint: Pubkey,

        pub hook_config: Pubkey,

        pub authority: Pubkey,
    }

    impl InitializeExtraAccountMetaListInstructionAccounts {
        pub fn new(
            payer: Pubkey,

            extra_account_meta_list: Pubkey,

            mint: Pubkey,

            hook_config: Pubkey,

            authority: Pubkey,
        ) -> Self {
            Self {
                payer,

                extra_account_meta_list,

                mint,

                hook_config,

                authority,
            }
        }
    }

    /// Instruction data for InitializeExtraAccountMetaList
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeExtraAccountMetaListInstructionData {}

    impl InitializeExtraAccountMetaListInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeExtraAccountMetaListInstruction
    impl InitializeExtraAccountMetaListInstruction {
        fn discriminator() -> [u8; 8] {
            [92u8, 197u8, 174u8, 197u8, 41u8, 124u8, 19u8, 3u8]
        }

        pub fn data(data: InitializeExtraAccountMetaListInstructionData) -> Self {
            Self {
                accounts: InitializeExtraAccountMetaListInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(
            mut self,
            accounts: InitializeExtraAccountMetaListInstructionAccounts,
        ) -> Self {
            self.accounts.payer = AccountMeta::new(accounts.payer, true);

            self.accounts.extra_account_meta_list =
                AccountMeta::new(accounts.extra_account_meta_list, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.hook_config = AccountMeta::new_readonly(accounts.hook_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.payer.clone());

            metas.push(self.accounts.extra_account_meta_list.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.hook_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializeHook
    // ....................................................................

    /// Main instruction struct for InitializeHook
    pub struct InitializeHookInstruction {
        pub accounts: InitializeHookInstructionAccountMetas,
        pub data: InitializeHookInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializeHook instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeHookInstructionAccountMetas {
        pub payer: AccountMeta,

        pub hook_config: AccountMeta,

        pub mint: AccountMeta,

        pub sss_config: AccountMeta,

        pub authority: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for InitializeHook instruction
    #[derive(Debug, Clone)]
    pub struct InitializeHookInstructionAccounts {
        pub payer: Pubkey,

        pub hook_config: Pubkey,

        pub mint: Pubkey,

        pub sss_config: Pubkey,

        pub authority: Pubkey,
    }

    impl InitializeHookInstructionAccounts {
        pub fn new(
            payer: Pubkey,

            hook_config: Pubkey,

            mint: Pubkey,

            sss_config: Pubkey,

            authority: Pubkey,
        ) -> Self {
            Self {
                payer,

                hook_config,

                mint,

                sss_config,

                authority,
            }
        }
    }

    /// Instruction data for InitializeHook
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeHookInstructionData {
        pub default_enabled: bool,
    }

    impl InitializeHookInstructionData {
        pub fn new(default_enabled: bool) -> Self {
            Self { default_enabled }
        }
    }

    /// Implementation for InitializeHookInstruction
    impl InitializeHookInstruction {
        fn discriminator() -> [u8; 8] {
            [37u8, 101u8, 119u8, 255u8, 156u8, 39u8, 252u8, 232u8]
        }

        pub fn data(data: InitializeHookInstructionData) -> Self {
            Self {
                accounts: InitializeHookInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeHookInstructionAccounts) -> Self {
            self.accounts.payer = AccountMeta::new(accounts.payer, true);

            self.accounts.hook_config = AccountMeta::new(accounts.hook_config, false);

            self.accounts.mint = AccountMeta::new_readonly(accounts.mint, false);

            self.accounts.sss_config = AccountMeta::new_readonly(accounts.sss_config, false);

            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.payer.clone());

            metas.push(self.accounts.hook_config.clone());

            metas.push(self.accounts.mint.clone());

            metas.push(self.accounts.sss_config.clone());

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

/// Custom struct: AccountFrozenEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AccountFrozenEvent {
    pub mint: Pubkey,

    pub account: Pubkey,

    pub by: Pubkey,

    pub timestamp: i64,
}

impl AccountFrozenEvent {
    pub fn new(mint: Pubkey, account: Pubkey, by: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            account,

            by,

            timestamp,
        }
    }
}

/// Custom struct: AccountThawedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AccountThawedEvent {
    pub mint: Pubkey,

    pub account: Pubkey,

    pub by: Pubkey,

    pub timestamp: i64,
}

impl AccountThawedEvent {
    pub fn new(mint: Pubkey, account: Pubkey, by: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            account,

            by,

            timestamp,
        }
    }
}

/// Custom struct: BlacklistEntry
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BlacklistEntry {
    pub mint: Pubkey,

    pub address: Pubkey,

    pub reason: String,

    pub added_by: Pubkey,

    pub added_at: i64,

    pub removed: bool,

    pub removed_by: Option<Pubkey>,

    pub removed_at: Option<i64>,

    pub bump: u8,
}

impl BlacklistEntry {
    pub fn new(
        mint: Pubkey,

        address: Pubkey,

        reason: String,

        added_by: Pubkey,

        added_at: i64,

        removed: bool,

        removed_by: Option<Pubkey>,

        removed_at: Option<i64>,

        bump: u8,
    ) -> Self {
        Self {
            mint,

            address,

            reason,

            added_by,

            added_at,

            removed,

            removed_by,

            removed_at,

            bump,
        }
    }
}

/// Custom struct: Blacklisted
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Blacklisted {
    pub mint: Pubkey,

    pub address: Pubkey,

    pub reason: String,

    pub blacklisted_by: Pubkey,

    pub timestamp: i64,
}

impl Blacklisted {
    pub fn new(
        mint: Pubkey,

        address: Pubkey,

        reason: String,

        blacklisted_by: Pubkey,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            address,

            reason,

            blacklisted_by,

            timestamp,
        }
    }
}

/// Custom struct: Burned
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Burned {
    pub mint: Pubkey,

    pub from: Pubkey,

    pub amount: u64,

    pub burner: Pubkey,

    pub new_total_supply: u64,

    pub timestamp: i64,
}

impl Burned {
    pub fn new(
        mint: Pubkey,

        from: Pubkey,

        amount: u64,

        burner: Pubkey,

        new_total_supply: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            from,

            amount,

            burner,

            new_total_supply,

            timestamp,
        }
    }
}

/// Custom struct: InitializeParams
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct InitializeParams {
    pub name: String,

    pub symbol: String,

    pub uri: String,

    pub decimals: u8,

    pub preset: StablecoinPreset,

    pub pauser: Pubkey,

    pub minter_authority: Pubkey,

    pub burner: Pubkey,

    pub enable_permanent_delegate: bool,

    pub enable_transfer_hook: bool,

    pub default_account_frozen: bool,

    pub blacklister: Option<Pubkey>,

    pub seizer: Option<Pubkey>,

    pub hook_program_id: Option<Pubkey>,
}

impl InitializeParams {
    pub fn new(
        name: String,

        symbol: String,

        uri: String,

        decimals: u8,

        preset: StablecoinPreset,

        pauser: Pubkey,

        minter_authority: Pubkey,

        burner: Pubkey,

        enable_permanent_delegate: bool,

        enable_transfer_hook: bool,

        default_account_frozen: bool,

        blacklister: Option<Pubkey>,

        seizer: Option<Pubkey>,

        hook_program_id: Option<Pubkey>,
    ) -> Self {
        Self {
            name,

            symbol,

            uri,

            decimals,

            preset,

            pauser,

            minter_authority,

            burner,

            enable_permanent_delegate,

            enable_transfer_hook,

            default_account_frozen,

            blacklister,

            seizer,

            hook_program_id,
        }
    }
}

/// Custom struct: Initialized
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Initialized {
    pub mint: Pubkey,

    pub preset: String,

    pub name: String,

    pub symbol: String,

    pub decimals: u8,

    pub master_authority: Pubkey,

    pub compliance_enabled: bool,

    pub timestamp: i64,
}

impl Initialized {
    pub fn new(
        mint: Pubkey,

        preset: String,

        name: String,

        symbol: String,

        decimals: u8,

        master_authority: Pubkey,

        compliance_enabled: bool,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            preset,

            name,

            symbol,

            decimals,

            master_authority,

            compliance_enabled,

            timestamp,
        }
    }
}

/// Custom struct: Minted
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Minted {
    pub mint: Pubkey,

    pub recipient: Pubkey,

    pub amount: u64,

    pub minter: Pubkey,

    pub new_total_supply: u64,

    pub timestamp: i64,
}

impl Minted {
    pub fn new(
        mint: Pubkey,

        recipient: Pubkey,

        amount: u64,

        minter: Pubkey,

        new_total_supply: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            recipient,

            amount,

            minter,

            new_total_supply,

            timestamp,
        }
    }
}

/// Custom struct: MinterConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct MinterConfig {
    pub mint: Pubkey,

    pub minter: Pubkey,

    pub is_active: bool,

    pub quota_per_period: u64,

    pub period_seconds: i64,

    pub minted_this_period: u64,

    pub period_start: i64,

    pub total_minted: u64,

    pub mint_count: u64,

    pub created_at: i64,

    pub bump: u8,
}

impl MinterConfig {
    pub fn new(
        mint: Pubkey,

        minter: Pubkey,

        is_active: bool,

        quota_per_period: u64,

        period_seconds: i64,

        minted_this_period: u64,

        period_start: i64,

        total_minted: u64,

        mint_count: u64,

        created_at: i64,

        bump: u8,
    ) -> Self {
        Self {
            mint,

            minter,

            is_active,

            quota_per_period,

            period_seconds,

            minted_this_period,

            period_start,

            total_minted,

            mint_count,

            created_at,

            bump,
        }
    }
}

/// Custom struct: PausedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PausedEvent {
    pub mint: Pubkey,

    pub by: Pubkey,

    pub timestamp: i64,
}

impl PausedEvent {
    pub fn new(mint: Pubkey, by: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            by,

            timestamp,
        }
    }
}

/// Custom struct: RemovedFromBlacklist
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RemovedFromBlacklist {
    pub mint: Pubkey,

    pub address: Pubkey,

    pub removed_by: Pubkey,

    pub timestamp: i64,
}

impl RemovedFromBlacklist {
    pub fn new(mint: Pubkey, address: Pubkey, removed_by: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            address,

            removed_by,

            timestamp,
        }
    }
}

/// Custom struct: RoleUpdate
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RoleUpdate {
    pub new_pauser: Option<Pubkey>,

    pub new_minter_authority: Option<Pubkey>,

    pub new_burner: Option<Pubkey>,

    pub new_blacklister: Option<Pubkey>,

    pub new_seizer: Option<Pubkey>,
}

impl RoleUpdate {
    pub fn new(
        new_pauser: Option<Pubkey>,

        new_minter_authority: Option<Pubkey>,

        new_burner: Option<Pubkey>,

        new_blacklister: Option<Pubkey>,

        new_seizer: Option<Pubkey>,
    ) -> Self {
        Self {
            new_pauser,

            new_minter_authority,

            new_burner,

            new_blacklister,

            new_seizer,
        }
    }
}

/// Custom struct: RoleUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RoleUpdated {
    pub mint: Pubkey,

    pub role: String,

    pub old_address: Pubkey,

    pub new_address: Pubkey,

    pub updated_by: Pubkey,

    pub timestamp: i64,
}

impl RoleUpdated {
    pub fn new(
        mint: Pubkey,

        role: String,

        old_address: Pubkey,

        new_address: Pubkey,

        updated_by: Pubkey,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            role,

            old_address,

            new_address,

            updated_by,

            timestamp,
        }
    }
}

/// Custom struct: Seized
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct Seized {
    pub mint: Pubkey,

    pub seized_from: Pubkey,

    pub seized_to: Pubkey,

    pub amount: u64,

    pub reason: String,

    pub seizer: Pubkey,

    pub timestamp: i64,
}

impl Seized {
    pub fn new(
        mint: Pubkey,

        seized_from: Pubkey,

        seized_to: Pubkey,

        amount: u64,

        reason: String,

        seizer: Pubkey,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            seized_from,

            seized_to,

            amount,

            reason,

            seizer,

            timestamp,
        }
    }
}

/// Custom struct: SeizureRecord
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct SeizureRecord {
    pub mint: Pubkey,

    pub seized_from: Pubkey,

    pub seized_to: Pubkey,

    pub amount: u64,

    pub reason: String,

    pub executed_by: Pubkey,

    pub executed_at: i64,

    pub transaction_signature: [u8; 64],

    pub bump: u8,
}

impl SeizureRecord {
    pub fn new(
        mint: Pubkey,

        seized_from: Pubkey,

        seized_to: Pubkey,

        amount: u64,

        reason: String,

        executed_by: Pubkey,

        executed_at: i64,

        transaction_signature: [u8; 64],

        bump: u8,
    ) -> Self {
        Self {
            mint,

            seized_from,

            seized_to,

            amount,

            reason,

            executed_by,

            executed_at,

            transaction_signature,

            bump,
        }
    }
}

/// Custom struct: StablecoinConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct StablecoinConfig {
    pub version: u8,

    pub preset: StablecoinPreset,

    pub mint: Pubkey,

    pub name: String,

    pub symbol: String,

    pub uri: String,

    pub decimals: u8,

    pub enable_permanent_delegate: bool,

    pub enable_transfer_hook: bool,

    pub default_account_frozen: bool,

    pub master_authority: Pubkey,

    pub pending_master_authority: Option<Pubkey>,

    pub pauser: Pubkey,

    pub minter_authority: Pubkey,

    pub burner: Pubkey,

    pub blacklister: Option<Pubkey>,

    pub seizer: Option<Pubkey>,

    pub hook_authority: Option<Pubkey>,

    pub paused: bool,

    pub total_supply: u64,

    pub total_minted_all_time: u64,

    pub total_burned_all_time: u64,

    pub blacklist_count: u32,

    pub hook_program_id: Option<Pubkey>,

    pub created_at: i64,

    pub last_updated_at: i64,

    pub bump: u8,
}

impl StablecoinConfig {
    pub fn new(
        version: u8,

        preset: StablecoinPreset,

        mint: Pubkey,

        name: String,

        symbol: String,

        uri: String,

        decimals: u8,

        enable_permanent_delegate: bool,

        enable_transfer_hook: bool,

        default_account_frozen: bool,

        master_authority: Pubkey,

        pending_master_authority: Option<Pubkey>,

        pauser: Pubkey,

        minter_authority: Pubkey,

        burner: Pubkey,

        blacklister: Option<Pubkey>,

        seizer: Option<Pubkey>,

        hook_authority: Option<Pubkey>,

        paused: bool,

        total_supply: u64,

        total_minted_all_time: u64,

        total_burned_all_time: u64,

        blacklist_count: u32,

        hook_program_id: Option<Pubkey>,

        created_at: i64,

        last_updated_at: i64,

        bump: u8,
    ) -> Self {
        Self {
            version,

            preset,

            mint,

            name,

            symbol,

            uri,

            decimals,

            enable_permanent_delegate,

            enable_transfer_hook,

            default_account_frozen,

            master_authority,

            pending_master_authority,

            pauser,

            minter_authority,

            burner,

            blacklister,

            seizer,

            hook_authority,

            paused,

            total_supply,

            total_minted_all_time,

            total_burned_all_time,

            blacklist_count,

            hook_program_id,

            created_at,

            last_updated_at,

            bump,
        }
    }
}

/// Custom enum: StablecoinPreset
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub enum StablecoinPreset {
    SSS1,

    SSS2,

    Custom,
}

/// Custom struct: UnpausedEvent
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct UnpausedEvent {
    pub mint: Pubkey,

    pub by: Pubkey,

    pub timestamp: i64,
}

impl UnpausedEvent {
    pub fn new(mint: Pubkey, by: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            by,

            timestamp,
        }
    }
}

/// Custom struct: AuthorityTransferProposed
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuthorityTransferProposed {
    pub mint: Pubkey,

    pub current: Pubkey,

    pub proposed: Pubkey,

    pub timestamp: i64,
}

impl AuthorityTransferProposed {
    pub fn new(mint: Pubkey, current: Pubkey, proposed: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            current,

            proposed,

            timestamp,
        }
    }
}

/// Custom struct: AuthorityTransferred
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AuthorityTransferred {
    pub mint: Pubkey,

    pub from: Pubkey,

    pub to: Pubkey,

    pub timestamp: i64,
}

impl AuthorityTransferred {
    pub fn new(mint: Pubkey, from: Pubkey, to: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            from,

            to,

            timestamp,
        }
    }
}

/// Custom struct: CpiMultiplierUpdated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct CpiMultiplierUpdated {
    pub mint: Pubkey,

    pub old_multiplier: u64,

    pub new_multiplier: u64,

    pub reference_month: String,

    pub data_source: String,

    pub updated_by: Pubkey,

    pub timestamp: i64,
}

impl CpiMultiplierUpdated {
    pub fn new(
        mint: Pubkey,

        old_multiplier: u64,

        new_multiplier: u64,

        reference_month: String,

        data_source: String,

        updated_by: Pubkey,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            old_multiplier,

            new_multiplier,

            reference_month,

            data_source,

            updated_by,

            timestamp,
        }
    }
}

/// Custom struct: FeedEntry
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct FeedEntry {
    pub symbol: String,

    pub switchboard_feed: Pubkey,

    pub feed_type: FeedType,

    pub base_currency: String,

    pub quote_currency: String,

    pub decimals: u8,

    pub active: bool,

    pub registered_at: i64,

    pub registered_by: Pubkey,
}

impl FeedEntry {
    pub fn new(
        symbol: String,

        switchboard_feed: Pubkey,

        feed_type: FeedType,

        base_currency: String,

        quote_currency: String,

        decimals: u8,

        active: bool,

        registered_at: i64,

        registered_by: Pubkey,
    ) -> Self {
        Self {
            symbol,

            switchboard_feed,

            feed_type,

            base_currency,

            quote_currency,

            decimals,

            active,

            registered_at,

            registered_by,
        }
    }
}

/// Custom struct: FeedRegistered
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct FeedRegistered {
    pub symbol: String,

    pub switchboard_feed: Pubkey,

    pub feed_type: String,

    pub base_currency: String,

    pub quote_currency: String,

    pub registered_by: Pubkey,

    pub timestamp: i64,
}

impl FeedRegistered {
    pub fn new(
        symbol: String,

        switchboard_feed: Pubkey,

        feed_type: String,

        base_currency: String,

        quote_currency: String,

        registered_by: Pubkey,

        timestamp: i64,
    ) -> Self {
        Self {
            symbol,

            switchboard_feed,

            feed_type,

            base_currency,

            quote_currency,

            registered_by,

            timestamp,
        }
    }
}

/// Custom struct: FeedRegistry
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct FeedRegistry {
    pub authority: Pubkey,

    pub feed_count: u8,

    pub feeds: Vec<FeedEntry>,

    pub bump: u8,
}

impl FeedRegistry {
    pub fn new(authority: Pubkey, feed_count: u8, feeds: Vec<FeedEntry>, bump: u8) -> Self {
        Self {
            authority,

            feed_count,

            feeds,

            bump,
        }
    }
}

/// Custom enum: FeedType
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub enum FeedType {
    Direct,

    Inverse,

    CpiIndexed,

    Custom {
        numerator: u64,

        denominator: u64,

        base_type: u8,
    },
}

/// Custom struct: GetQuoteParams
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GetQuoteParams {
    pub input_amount: u64,

    pub min_output: u64,

    pub nonce: u64,
}

impl GetQuoteParams {
    pub fn new(input_amount: u64, min_output: u64, nonce: u64) -> Self {
        Self {
            input_amount,

            min_output,

            nonce,
        }
    }
}

/// Custom struct: InitializeOracleParams
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct InitializeOracleParams {
    pub feed_symbol: String,

    pub description: String,

    pub max_staleness_secs: i64,

    pub mint_fee_bps: u16,

    pub redeem_fee_bps: u16,

    pub max_confidence_bps: u16,

    pub quote_validity_secs: i64,

    pub cpi_multiplier: u64,

    pub cpi_min_update_interval: i64,

    pub cpi_data_source: String,
}

impl InitializeOracleParams {
    pub fn new(
        feed_symbol: String,

        description: String,

        max_staleness_secs: i64,

        mint_fee_bps: u16,

        redeem_fee_bps: u16,

        max_confidence_bps: u16,

        quote_validity_secs: i64,

        cpi_multiplier: u64,

        cpi_min_update_interval: i64,

        cpi_data_source: String,
    ) -> Self {
        Self {
            feed_symbol,

            description,

            max_staleness_secs,

            mint_fee_bps,

            redeem_fee_bps,

            max_confidence_bps,

            quote_validity_secs,

            cpi_multiplier,

            cpi_min_update_interval,

            cpi_data_source,
        }
    }
}

/// Custom struct: OracleConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OracleConfig {
    pub version: u8,

    pub mint: Pubkey,

    pub authority: Pubkey,

    pub pending_authority: Option<Pubkey>,

    pub feed_symbol: String,

    pub description: String,

    pub max_staleness_secs: i64,

    pub mint_fee_bps: u16,

    pub redeem_fee_bps: u16,

    pub max_confidence_bps: u16,

    pub quote_validity_secs: i64,

    pub cpi_multiplier: u64,

    pub cpi_last_updated: i64,

    pub cpi_min_update_interval: i64,

    pub cpi_data_source: String,

    pub paused: bool,

    pub pause_reason: String,

    pub total_minted_usd: u64,

    pub total_redeemed_usd: u64,

    pub total_fees_collected: u64,

    pub created_at: i64,

    pub last_updated_at: i64,

    pub bump: u8,
}

impl OracleConfig {
    pub fn new(
        version: u8,

        mint: Pubkey,

        authority: Pubkey,

        pending_authority: Option<Pubkey>,

        feed_symbol: String,

        description: String,

        max_staleness_secs: i64,

        mint_fee_bps: u16,

        redeem_fee_bps: u16,

        max_confidence_bps: u16,

        quote_validity_secs: i64,

        cpi_multiplier: u64,

        cpi_last_updated: i64,

        cpi_min_update_interval: i64,

        cpi_data_source: String,

        paused: bool,

        pause_reason: String,

        total_minted_usd: u64,

        total_redeemed_usd: u64,

        total_fees_collected: u64,

        created_at: i64,

        last_updated_at: i64,

        bump: u8,
    ) -> Self {
        Self {
            version,

            mint,

            authority,

            pending_authority,

            feed_symbol,

            description,

            max_staleness_secs,

            mint_fee_bps,

            redeem_fee_bps,

            max_confidence_bps,

            quote_validity_secs,

            cpi_multiplier,

            cpi_last_updated,

            cpi_min_update_interval,

            cpi_data_source,

            paused,

            pause_reason,

            total_minted_usd,

            total_redeemed_usd,

            total_fees_collected,

            created_at,

            last_updated_at,

            bump,
        }
    }
}

/// Custom struct: OracleInitialized
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OracleInitialized {
    pub mint: Pubkey,

    pub authority: Pubkey,

    pub feed_symbol: String,

    pub mint_fee_bps: u16,

    pub redeem_fee_bps: u16,

    pub timestamp: i64,
}

impl OracleInitialized {
    pub fn new(
        mint: Pubkey,

        authority: Pubkey,

        feed_symbol: String,

        mint_fee_bps: u16,

        redeem_fee_bps: u16,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            authority,

            feed_symbol,

            mint_fee_bps,

            redeem_fee_bps,

            timestamp,
        }
    }
}

/// Custom struct: OracleMint
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OracleMint {
    pub mint: Pubkey,

    pub recipient: Pubkey,

    pub usd_amount: u64,

    pub token_amount: u64,

    pub fee_amount: u64,

    pub price_used: u64,

    pub feed_symbol: String,

    pub timestamp: i64,
}

impl OracleMint {
    pub fn new(
        mint: Pubkey,

        recipient: Pubkey,

        usd_amount: u64,

        token_amount: u64,

        fee_amount: u64,

        price_used: u64,

        feed_symbol: String,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            recipient,

            usd_amount,

            token_amount,

            fee_amount,

            price_used,

            feed_symbol,

            timestamp,
        }
    }
}

/// Custom struct: OraclePaused
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OraclePaused {
    pub mint: Pubkey,

    pub paused: bool,

    pub reason: String,

    pub by: Pubkey,

    pub timestamp: i64,
}

impl OraclePaused {
    pub fn new(mint: Pubkey, paused: bool, reason: String, by: Pubkey, timestamp: i64) -> Self {
        Self {
            mint,

            paused,

            reason,

            by,

            timestamp,
        }
    }
}

/// Custom struct: OracleRedeem
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct OracleRedeem {
    pub mint: Pubkey,

    pub redeemer: Pubkey,

    pub token_amount: u64,

    pub usd_amount: u64,

    pub fee_amount: u64,

    pub price_used: u64,

    pub feed_symbol: String,

    pub timestamp: i64,
}

impl OracleRedeem {
    pub fn new(
        mint: Pubkey,

        redeemer: Pubkey,

        token_amount: u64,

        usd_amount: u64,

        fee_amount: u64,

        price_used: u64,

        feed_symbol: String,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            redeemer,

            token_amount,

            usd_amount,

            fee_amount,

            price_used,

            feed_symbol,

            timestamp,
        }
    }
}

/// Custom struct: PendingQuote
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PendingQuote {
    pub mint: Pubkey,

    pub requester: Pubkey,

    pub direction: QuoteDirection,

    pub feed_symbol: String,

    pub input_amount: u64,

    pub output_amount: u64,

    pub fee_amount: u64,

    pub price_snapshot: u64,

    pub valid_until: i64,

    pub min_output: u64,

    pub used: bool,

    pub created_at: i64,

    pub nonce: u64,

    pub bump: u8,
}

impl PendingQuote {
    pub fn new(
        mint: Pubkey,

        requester: Pubkey,

        direction: QuoteDirection,

        feed_symbol: String,

        input_amount: u64,

        output_amount: u64,

        fee_amount: u64,

        price_snapshot: u64,

        valid_until: i64,

        min_output: u64,

        used: bool,

        created_at: i64,

        nonce: u64,

        bump: u8,
    ) -> Self {
        Self {
            mint,

            requester,

            direction,

            feed_symbol,

            input_amount,

            output_amount,

            fee_amount,

            price_snapshot,

            valid_until,

            min_output,

            used,

            created_at,

            nonce,

            bump,
        }
    }
}

/// Custom enum: QuoteDirection
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub enum QuoteDirection {
    Mint,

    Redeem,
}

/// Custom struct: QuoteGenerated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct QuoteGenerated {
    pub quote_id: Pubkey,

    pub mint: Pubkey,

    pub feed_symbol: String,

    pub direction: String,

    pub input_amount: u64,

    pub output_amount: u64,

    pub fee_amount: u64,

    pub price_used: u64,

    pub valid_until: i64,

    pub timestamp: i64,
}

impl QuoteGenerated {
    pub fn new(
        quote_id: Pubkey,

        mint: Pubkey,

        feed_symbol: String,

        direction: String,

        input_amount: u64,

        output_amount: u64,

        fee_amount: u64,

        price_used: u64,

        valid_until: i64,

        timestamp: i64,
    ) -> Self {
        Self {
            quote_id,

            mint,

            feed_symbol,

            direction,

            input_amount,

            output_amount,

            fee_amount,

            price_used,

            valid_until,

            timestamp,
        }
    }
}

/// Custom struct: QuoteResult
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct QuoteResult {
    pub output_amount: u64,

    pub fee_amount: u64,

    pub price_used: u64,

    pub valid_until: i64,

    pub quote_account: Pubkey,
}

impl QuoteResult {
    pub fn new(
        output_amount: u64,

        fee_amount: u64,

        price_used: u64,

        valid_until: i64,

        quote_account: Pubkey,
    ) -> Self {
        Self {
            output_amount,

            fee_amount,

            price_used,

            valid_until,

            quote_account,
        }
    }
}

/// Custom struct: RegisterFeedParams
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct RegisterFeedParams {
    pub symbol: String,

    pub feed_type: FeedType,

    pub base_currency: String,

    pub quote_currency: String,

    pub decimals: u8,
}

impl RegisterFeedParams {
    pub fn new(
        symbol: String,

        feed_type: FeedType,

        base_currency: String,

        quote_currency: String,

        decimals: u8,
    ) -> Self {
        Self {
            symbol,

            feed_type,

            base_currency,

            quote_currency,

            decimals,
        }
    }
}

/// Custom struct: UpdateCpiParams
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct UpdateCpiParams {
    pub new_multiplier: u64,

    pub reference_month: String,

    pub data_source: String,
}

impl UpdateCpiParams {
    pub fn new(new_multiplier: u64, reference_month: String, data_source: String) -> Self {
        Self {
            new_multiplier,

            reference_month,

            data_source,
        }
    }
}

/// Custom struct: HookConfig
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct HookConfig {
    pub mint: Pubkey,

    pub sss_core_program: Pubkey,

    pub authority: Pubkey,

    pub enabled: bool,

    pub transfer_count: u64,

    pub blocked_count: u64,

    pub bump: u8,
}

impl HookConfig {
    pub fn new(
        mint: Pubkey,

        sss_core_program: Pubkey,

        authority: Pubkey,

        enabled: bool,

        transfer_count: u64,

        blocked_count: u64,

        bump: u8,
    ) -> Self {
        Self {
            mint,

            sss_core_program,

            authority,

            enabled,

            transfer_count,

            blocked_count,

            bump,
        }
    }
}
/// Custom struct: TransferBlocked
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TransferBlocked {
    pub mint: Pubkey,

    pub blocked_address: Pubkey,

    pub reason: String,

    pub amount: u64,

    pub timestamp: i64,
}

impl TransferBlocked {
    pub fn new(
        mint: Pubkey,

        blocked_address: Pubkey,

        reason: String,

        amount: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            blocked_address,

            reason,

            amount,

            timestamp,
        }
    }
}

/// Custom struct: TransferValidated
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct TransferValidated {
    pub mint: Pubkey,

    pub source: Pubkey,

    pub destination: Pubkey,

    pub amount: u64,

    pub timestamp: i64,
}

impl TransferValidated {
    pub fn new(
        mint: Pubkey,

        source: Pubkey,

        destination: Pubkey,

        amount: u64,

        timestamp: i64,
    ) -> Self {
        Self {
            mint,

            source,

            destination,

            amount,

            timestamp,
        }
    }
}

// ============================================================================
// END OF GENERATED FILE
// ============================================================================
